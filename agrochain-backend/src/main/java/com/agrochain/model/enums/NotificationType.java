package com.agrochain.model.enums;

/**
 * Categories for in-app notifications (orders, chat, admin).
 */
public enum NotificationType {
    ORDER_UPDATE,
    NEW_MESSAGE,
    SYSTEM,
    ADMIN_ALERT
}
